within ;

package NcDataReader2 "Library to read interpolated data from netCDF files and DAP servers"
  extends Modelica.Icons.Package;
  annotation(uses(Modelica(version="3.2.1")));
end NcDataReader2;
